/**
 * When making a new command, you must export it in this index.ts file
 * To create a command, follow the similar structure to the other command files found in commandFiles directory
 */

export * as commandslist from './commandslist';
export * as monitorbots from './monitorbots';
